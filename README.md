# napi experiment

This repository demonstrates that the core crypto database can be accessed on nodeJS. We achieve this by using napi-rs.

## Build and run

1. Install `yarn`
1. Install dependencies: `yarn install`
1. Build the project: `yarn build`
1. Run the test: `yarn test`
